build/
*.o
*.dSYM/
.DS_Store

# Swift Package Manager and Xcode
.build/
.swiftpm/
DerivedData/
xcuserdata/
*.xcuserstate

# Recorded traces; the committed fixtures under tests/traces are generated by
# `make traces` and are tracked deliberately.
/*.lstrace
